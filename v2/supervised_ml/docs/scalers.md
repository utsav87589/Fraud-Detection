### ------------ Folder name : scalers --------------------

- here is all the information about the 'scalers' folder including the files and the operation each individual file/subfolder is responsible for, alongside the folder structure.

- in the scaler folder, we have the saved scaler object (scaler.pkl) that was used for the numerical column for the distnce based models. 

- we used the MinMax scaler with the feature range (0, 1).