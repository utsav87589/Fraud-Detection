### ------------ Folder name : flowcharts --------------------

- here is all the information about the 'flowcharts' folder including the files and the operation each individual file/subfolder is responsible for, alongside the folder structure.

- in the flowchart folder, we have the flowchart for the entire supervised ML workflow, it includes the steps from the Feature engineering logic to predcitions and the final reporting part.

- the flowchart itself is very self explanatory, with using the mutlitple arrows and poiting out the repititive and odd process that were done in context to the the project.

- to really understand in deep, please refer to the individual notebooks/ src files, however, the flowchart itself is very self explanatory